generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "postgresql"
}

model Trip {
  id          String   @id @default(uuid())
  title       String
  description String?
  price       Float
  createdAt   DateTime @default(now())
}
